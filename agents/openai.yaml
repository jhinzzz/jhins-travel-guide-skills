interface:
  display_name: "Travel Itinerary Redesign"
  short_description: "Weather-aware travel planning with hotels and outputs."
  default_prompt: "Use $travel-itinerary-redesign to build a weather-aware travel plan, ask for dates, destination, purpose, food preferences, and budget first, then add hotel tiers, packing, and final markdown plus HTML outputs."
policy:
  allow_implicit_invocation: true
