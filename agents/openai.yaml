interface:
  display_name: "Travel Itinerary Redesign"
  short_description: "Weather-aware travel guide redesign and planning."
  default_prompt: "Use $travel-itinerary-redesign to turn a trip brief or existing travel page into a weather-aware itinerary guide; ask for dates, destination, purpose, food preferences, and budget first, then add hotel tiers and only produce markdown and HTML deliverables when the user wants a shareable guide or page."
policy:
  allow_implicit_invocation: true
